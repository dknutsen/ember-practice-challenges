{{page-title "PracticeChallenges"}}

<div class="flex h-screen">
  <div class="h-full overflow-y-hidden shadow-lg">
    <Sidebar />
  </div>
  <div class="flex-1 h-full overflow-y-hidden">
    <ExampleRenderer @currentExample={{this.id}} />
  </div>
  {{outlet}}
</div>
