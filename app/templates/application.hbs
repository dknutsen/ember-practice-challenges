{{page-title "PracticeChallenges"}}

<div class="flex h-screen">
  <div class="h-full overflow-y-scroll shadow-lg">
    <Sidebar />
  </div>
  <div class="flex-1 h-full overflow-y-scroll">
    <ChallengeRenderer @currentChallenge={{this.id}}>
      {{outlet}}
    </ChallengeRenderer>
  </div>
</div>